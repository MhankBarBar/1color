{
	"name": "1color",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"description": "Keep one color in a photo, let the rest fall to black and white.",
	"homepage": "https://1color.mhankbarbar.dev",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/MhankBarBar/1color.git"
	},
	"author": "MhankBarBar",
	"license": "MIT",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"test": "tsx --test src/lib/*.test.ts",
		"typecheck": "tsc -p tsconfig.app.json && tsc -p tsconfig.json && svelte-check --tsconfig ./tsconfig.app.json",
		"deploy": "npm run build && npx --yes wrangler pages deploy dist --project-name 1color --force"
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^7.3.0",
		"@types/node": "^26.6.2",
		"svelte": "^5.57.1",
		"svelte-check": "^4.7.6",
		"tsx": "^4.23.15",
		"typescript": "^6.0.3",
		"vite": "^8.3.0"
	}
}
